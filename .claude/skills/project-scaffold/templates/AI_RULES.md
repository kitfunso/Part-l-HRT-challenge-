# AI Rules — <PROJECT NAME>

<!-- The non-negotiables. This is quality control: the AI must follow every
     rule here on every change. Think of it as briefing your engineer. -->

## Stack constraints
<!-- e.g. "SwiftUI only — no UIKit", "TypeScript strict mode on",
     "no new dependencies without explicit approval". -->
-

## Code organization
<!-- Files go where ARCHITECTURE.md says. Naming conventions, max file size,
     module boundaries. -->
-

## Security
<!-- e.g. "secrets live in Keychain / environment variables only — never
     hardcoded, never committed". -->
-

## Quality bar
<!-- Testing expectations, error handling, and what "done" means for a unit
     of work. -->
-

## Forbidden
<!-- Patterns, libraries, or shortcuts that must never be used. -->
-

## Workflow
- Follow `docs/PLAN.md` one step at a time; never skip ahead.
- New scope goes into `docs/PRD.md` before it gets built.
