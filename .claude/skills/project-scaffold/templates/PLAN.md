# Build Plan — <PROJECT NAME>

<!-- The step-by-step roadmap. Work ONE step at a time: finish it, check it
     off, confirm with the user, then move to the next. This is what keeps
     the build from getting lost in a sprawl of half-done code. -->

## How to use this plan
- Do the current step only — the first one whose checkbox is unchecked.
- A step is done when its "Done when" check passes.
- Mark the checkbox `[x]`, confirm with the user, then start the next step.

## Steps

### [ ] Step 1 — <title>
- Goal:
- Files:
- Done when:

### [ ] Step 2 — <title>
- Goal:
- Files:
- Done when:

<!-- Add as many steps as needed. Keep each one small and independently
     verifiable — if a step is hard to check, split it. -->
